<!DOCTYPE html>
<html>
  <head>
    <title>select</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    
    <table border="1">
      <thead>
        <tr>
          <th>물품번호</th>
          <th>물품명</th>
          <th>물품가격</th>
          <th>물품수량</th>
          <th>비고</th>
        </tr>
      </thead>

      <tbody>
      <form action="/mongo/update" method="post">
      <% for(var i=0;i<list.length;i++) { %>
        <tr>
          <td><input type="text" name="no" value="<%= list[i].no %>"/> </td>
          <td><input type="text" name="na" value="<%= list[i].name %>" /></td>
          <td><input type="text" name="pr" value="<%= list[i].price %>" /></td>
          <td><input type="text" name="cn" value="<%= list[i].cnt %>" /></td>
          <td><input type="submit" value="수정" /></td>
        </tr>
      <% } %>  
      </form>
      </tbody>
  </table>  
  </body>
</html>